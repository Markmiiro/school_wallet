// TEMPORARY placeholder — will be replaced with the real Stitch-styled
// Login screen in a later step. This exists now just so app_router.dart
// has something to compile against.

import 'package:flutter/material.dart';

class LoginScreen extends StatelessWidget {
  const LoginScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Login Screen (placeholder)')),
    );
  }
}